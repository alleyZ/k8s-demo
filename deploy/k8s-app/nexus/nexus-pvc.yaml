apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-20g-nexus
  namespace: data-cube
  labels:
    pvc: nexus
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  selector:
    matchLabels:
      use: nexus