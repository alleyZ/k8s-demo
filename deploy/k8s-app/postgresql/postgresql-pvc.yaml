apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-10g-postgresql
  namespace: data-cube
  labels:
    pvc: postgresql
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  selector:
    matchLabels:
      use: postgresql